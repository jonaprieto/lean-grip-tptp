# TODO

- [ ] Add typed `tff`/`thf`/`tcf` formula layers without weakening the raw envelope.
- [ ] Add a TSTP derivation/proof annotation AST for consumers that need provenance.
- [ ] Add corpus fixtures from representative ATP output and upstream TPTP examples.
- [ ] Add a streaming adapter only if a concrete caller needs inputs larger than memory.
